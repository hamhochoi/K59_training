1. -Dùng flask xây dựng một webserver chạy cổng 5000 (localhost:5000)
   -Chạy webserver trên

2. Dùng ngrok tạo một tunel vào cổng 5000 (./ngrok http 5000). Xem chi tiết trong /ngrok/read_me.txt

3. Xem địa chỉ của ngrok tại "curl http://127.0.0.1:4040/api/tunnels"
